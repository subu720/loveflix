# Audio Folder

Place your audio files (`.mp3`, `.wav`) in this folder.

You can reference them in your `src/app/data/mediaData.ts` file or in `src/app/App.tsx` using absolute web paths like:
`"/assets/audio/background-music.mp3"`
